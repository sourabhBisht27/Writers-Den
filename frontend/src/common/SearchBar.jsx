import "./SearchBar.css";

const SarchBar = () => {
  return (
    <div>
      <input
        className="search-bar"
        type="text"
        placeholder="Search..."
      />
    </div>
  );
};

export default SarchBar;
